#ifndef HEADER_gubg_Range_macro_hpp_ALREADY_INCLUDED
#define HEADER_gubg_Range_macro_hpp_ALREADY_INCLUDED

#ifdef RANGE
#error RANGE already defined
#endif
#define RANGE(ctr) (ctr).begin(), (ctr).end()

#endif
